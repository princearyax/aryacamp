<% layout("./layouts/boilerplate") %>

<div id="cluster-map"> kkhfchdivc</div>

    <h1>All campgrounds</h1>
    <div>
        <a href="/campgrounds/new">Create new</a>
    </div>

    <% campgrounds.forEach(campground=> { %>
        <div class="card mb-2">
            <div class="row">
                <div class="col-md-4">
                    <!-- <img src="https://picsum.photos/id/<%= Math.floor(Math.random()*200) %>/450/300" class="img-fluid" alt=""> -->
                    <% if(campground.images.length) {%>
                        <img crossorigin="anonymous" src="<%= campground.images[0].url %>" class="img-fluid" alt="">
                    <% }else { %>
                        <img crossorigin="anonymous" class="img-fluid" src="somedefaultimg" alt="">
                    <% } %>
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title"> 
                            <%= campground.title %>
                        </h5>
                        <p class="card-text">
                            <%= campground.description %>
                        </p>
                        <p class="card-text">
                            <small class="text-muted">
                                <%= campground.location %>
                            </small>
                        </p>
                        <a href="/campgrounds/<%= campground._id %>" class="btn btn-secondary">View</a>
                    </div>
                </div>
            </div>
        </div>
        <% }); %>

<script>
    const maptilerApiKey = '<%- process.env.MAPTILER_API_KEY %>';
</script>


<%-"<script>"%>
const campgrounds = {"features":<%- JSON.stringify(campgrounds); %>}
<%-"</script>"%>

<script src="/scripts/clusterMap.js"></script>





